{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@arkitect-ui/react-example",
  "sourceRoot": "apps/react-example/src",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "nx-oxlint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/react-example/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "apps/react-example"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": false,
        "reportsDirectory": "{workspaceRoot}/coverage/apps/react-example"
      }
    }
  },
  "tags": ["type:application", "scope:react-example"]
}
